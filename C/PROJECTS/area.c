#include<stdio.h>
#include<math.h>
int main()
{
    int length, width;
    printf("enter length");
    scanf("%d",  &length );
    printf("enter width");
    scanf("%d", &width);
    printf("your area is: %d", length*width);
    return 0;
}